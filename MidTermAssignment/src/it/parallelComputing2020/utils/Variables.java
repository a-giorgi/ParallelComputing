package it.parallelComputing2020.utils;

public class Variables {
    public static final int maxSizeAllowed = 0; //max size limit (no limit if this value is <= 0)
    public static int numThreads = 8;
}
